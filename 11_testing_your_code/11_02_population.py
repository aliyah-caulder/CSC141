# 11-2 involved modifying 11-1 so the city_functions function had an optional parameter for population.
# It also added another test for input that had the population argument.
# Both tests return passed.
# Difficulty was a 7 because I had some trouble with the optional argument.